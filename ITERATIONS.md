# Vibe Todo 迭代规划

## 迭代原则
- ⏱️ **周期**：每 2.5 小时一次迭代
- 🎯 **目标**：小步快跑，稳步演进
- 🧪 **TDD**：测试驱动开发，质量第一
- 📝 **记录**：每次迭代都有规划、执行、总结

---

## 迭代历史

### 迭代 #1：v0.3.0 - 视图增强（第一阶段）✅ **已完成**

**规划时间**：2026-03-02
**实际周期**：~2小时
**目标**：实现任务卡片视图

#### 完成内容
1. ✅ **设计任务卡片视图**
   - 完整的任务信息展示（标题、状态、优先级、项目、描述、标签、截止日期、工时、ID）
   - 使用 Rich Panel 进行美观的边框包装
   - 富文本格式和颜色

2. ✅ **实现卡片视图渲染**
   - `TaskCardView` 类
   - `render_task()` 和 `display_task()` 方法
   - `render_list()` 和 `display_list()` 方法

3. ✅ **添加 `--view` 选项到 `vibe list`**
   - 支持 `table`（默认）、`card`、`timeline`、`board` 四种视图
   - 保持默认表格视图完全不变
   - 新增卡片视图选项

4. ✅ **完整的单元测试**
   - `tests/test_views.py` - 8个新测试
   - 测试卡片渲染
   - 测试视图切换
   - 测试边界情况

5. ✅ **更新文档和 CHANGELOG**
   - 更新 CHANGELOG.md
   - 更新 pyproject.toml 版本号到 0.3.0

#### 验收标准
- [x] `vibe list --view card` 可以正常工作 ✅
- [x] 卡片视图美观且信息完整 ✅
- [x] 所有测试通过（79通过，7跳过）✅
- [x] 保持向后兼容 ✅

#### 总结
- 迭代顺利完成，比预计时间提前
- TDD 流程执行良好（先写测试，再实现）
- 发现并解决了循环导入问题
- 为后续视图（时间线、看板）打下了基础

---

---

### 迭代 #2：v0.3.0 - 视图增强（第二阶段）⏳ **待开始**

**规划时间**：2026-03-02
**预计周期**：2.5小时
**目标**：实现时间线视图

#### 规划内容
1. **设计时间线视图**
   - 按什么时间分组？（创建时间、更新时间、截止日期）
   - 时间线布局和样式

2. **实现时间线视图渲染**
   - `TaskTimelineView` 类完善
   - 按时间分组展示
   - 时间线标记和连接线

3. **测试 `--view timeline` 选项**
   - 确保时间线视图正常工作
   - 测试不同时间范围的数据

4. **完整的单元测试**
   - 测试时间线渲染
   - 测试时间分组逻辑

5. **更新文档和 CHANGELOG**

#### 验收标准
- [ ] `vibe list --view timeline` 可以正常工作
- [ ] 时间线视图清晰且美观
- [ ] 所有测试通过（至少 79 个通过）
- [ ] 保持向后兼容

---

### 迭代 #3：v0.3.0 - 视图增强（第三阶段）📋 **待规划**
- 看板视图
- 按状态列展示
